/**
 * The `/admin` route now renders `DashboardPage` (registrations + session
 * summary). This file is kept as a no-op stub in case future routes need
 * a CMS workshop list. Safe to delete if no other module imports it.
 */
export function WorkshopsPage() {
  return null;
}
